/*
NOTE: This test file is currently commented out as it was designed for the previous Anthropic API implementation.
It needs to be updated to test error handling for the OpenRouter/OpenAI SDK implementation.

import fs from 'fs';
import path from 'path';
import { jest } from '@jest/globals';
import mock from 'mock-fs';
import { handleClaudeError } from '../modules/ai-services.js';

describe('handleClaudeError', () => {
  // ... existing tests ...
});
*/ 